import { render, screen } from '@testing-library/react';
import React from 'react';
import EventCard from '..';

describe('TempComponent', () => {
  it('should show the Temp Component when the component is rendered', () => {});
});
